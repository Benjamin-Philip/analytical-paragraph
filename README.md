# English Analytical Project

This a repo for some scripts to generate my project.

Install:

- `npm`
- `rake`

Then run:

```bash
git clone https://github.com/Benjamin-Philip/analytical-paragraph.git
cd analytical-paragraph
npm install
rake
```

